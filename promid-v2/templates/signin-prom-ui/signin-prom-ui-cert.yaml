apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: signin-prom-ui-cert
spec:
  secretName: signin-prom-ui-cert
  dnsNames:
    - {{ .Values.domainSignInPromUI }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
