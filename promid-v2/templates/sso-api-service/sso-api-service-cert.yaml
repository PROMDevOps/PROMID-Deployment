apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sso-api-service-cert
spec:
  secretName: sso-api-service-cert
  dnsNames:
    - {{ .Values.domainSsoApiService }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
