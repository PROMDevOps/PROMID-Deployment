apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sso-api-service-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.org/client-max-body-size: 50m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.domainSsoApiService }}
      secretName: sso-api-service-cert
  rules:
    - host: {{ .Values.domainSsoApiService }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: promid-{{ .Values.envAlias }}-sso-api-service
                port:
                  number: 80
