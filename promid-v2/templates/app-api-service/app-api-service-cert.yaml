apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: app-api-service-cert
spec:
  secretName: app-api-service-cert
  dnsNames:
    - {{ .Values.domainAppApiService }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
