apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: admin-prom-ui-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.org/client-max-body-size: 50m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.domainAdminPromUI }}
      secretName: admin-prom-ui-cert
  rules:
    - host: {{ .Values.domainAdminPromUI }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: promid-{{ .Values.envAlias }}-admin-prom-ui
                port:
                  number: 80
